# Set this server-side only. Never put the key in site.js or commit .env.local.
RESEND_API_KEY=
# The address must use a domain verified in your Resend account.
RESEND_FROM_EMAIL="Zykken Website <website@zykken.com>"
